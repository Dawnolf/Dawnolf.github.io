
풀이 시간 약 30분정도?
###### 문제 설명

문자열 s를 숫자로 변환한 결과를 반환하는 함수, solution을 완성하세요.

##### 제한 조건

- s의 길이는 1 이상 5이하입니다.
- s의 맨앞에는 부호(+, -)가 올 수 있습니다.
- s는 부호와 숫자로만 이루어져있습니다.
- s는 "0"으로 시작하지 않습니다.

##### 입출력 예

예를들어 str이 "1234"이면 1234를 반환하고, "-1234"이면 -1234를 반환하면 됩니다.  
str은 부호(+,-)와 숫자로만 구성되어 있고, 잘못된 값이 입력되는 경우는 없습니다.

## 풀이

```cpp
#include <bits/stdc++.h>

using namespace std;

int solution(string s) {
    int answer = 0;
    int tmp =0;
    bool isMinus = false;
    int i=0;
     if (s[0] == '-') {
     isMinus = true;
     i = 1;
    }
     else if(s[0]=='+'){
         i = 1;
     }

   // if (s[i] == '0') i++;
    
// 테스트 케이스 의심할만한 구간 -01234
    for(i; i<s.length(); i++){
        tmp+=(s[i]-'0');
        if(i==s.length()-1) break;
        tmp*=10;
    }
    
    if(isMinus) answer= -tmp;
    else answer= tmp;
    return answer;
}
```

시간이 좀 오래걸렸는데 
다른 사람 풀이를 볼까하다가 참고 풀었습니다. (다행)

'-'만 체크 했던게 문제였었습니다. 글을 잘 읽지 않고 양수 표기할 때 일상에서는 '+'를 잘 쓰지 않으니까 하고 있었는데
생각해보니 string은 '+' 붙여도 상관 없다는 사실을.. 늦게 깨달았습니다

```cpp
// 이 부분이 없었음.. 처음 풀었을 때
else if(s[0]=='+'){
         i = 1;
     }
```

저 주석은 혹시 s는 <span style="yellow">"0"으로 시작하지 않습니다.</span> 라고 했는데
혹시..? 말장난으로 s앞이랬지 '-'뒤에 0이 오지말란 법은 없으니까.. 
이런식으로 머리 굴리다가 시간만 버린 결과물입니다. 

## 다른 사람 풀이

```cpp
#include <string>
#include <vector>
using namespace std;

int solution(string s) {
    int answer = stoi(s);
    return answer;
}
```

.. stoi라는 함수 평소에 자주 썼는데 알고리즘 너무 오랜만이다 보니까 쓸 생각을 못했었습니다.
그리고 부호를 바꿔줄 수 있다는건 처음 알았..

이번 추석에는 stoi 함수에 대해서 공부해보는게 좋을것 같습니다.